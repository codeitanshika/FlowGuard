# Aggregated dependencies for ALL services and agents, for local development
# and CI's unit-test job from a single virtualenv at the repo root (see
# docs/deployment/setup-guide.md). CI installs exactly this file, so anything
# the unit tests import must be listed here.
# Each service's own pyproject.toml lists only what that service actually
# uses — Phase 3's per-service Dockerfiles install from those, not this file.

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
httpx>=0.27
redis>=5.0
structlog>=24.1
pyjwt>=2.9
bcrypt>=4.2
anthropic>=1.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
pytest>=8.0
pytest-asyncio>=0.24
