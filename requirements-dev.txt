# Aggregated dependencies for ALL services, for local Phase 1/2 development
# from a single virtualenv at the repo root (see docs/deployment/setup-guide.md).
# Each service's own pyproject.toml lists only what that service actually
# uses — Phase 3's per-service Dockerfiles install from those, not this file.

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
httpx>=0.27
redis>=5.0
structlog>=24.1
pyjwt>=2.9
bcrypt>=4.2
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
